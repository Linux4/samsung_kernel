dtb-y += exynos7870-gtanotexlwifi_kor_open_00.dtb
dtb-y += exynos7870-gtanotexlwifi_kor_open_02.dtb

targets += dtbs
DTB_LIST  := $(dtb-y) $(dtbo-y)

dtbs: $(addprefix $(obj)/, $(DTB_LIST))

clean-files := *.dtb*
