# SPDX-License-Identifier: GPL-2.0-only
config SENSORS_SX932X
    tristate "GRIP_SENSOR"
    depends on I2C
    default n
    help
      Say Y to enable the SAR detection application, if N here not to use the
      this SAR sensor
